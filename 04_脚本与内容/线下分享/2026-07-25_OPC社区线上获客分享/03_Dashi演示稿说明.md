# Dashi 演示稿说明

更新日期：2026-07-22  
状态：8 页绿色轻拟态版本已完成本轮视觉与素材更新

## 当前主版本

- 主题：Dashi `theme01` 绿色轻拟态风格，已清理多余边角装饰。
- 页数：8 页。
- 标题：《如何通过线上运营，实现产品与服务的销售目标》。
- 主讲人：孙轩。
- 案例：武汉驾到短视频业务的线上获客全链路。
- HTML：`output/opc-business-sharing-theme01-v3-green-editable/ppt/index.html`。
- 内容计划：`output/opc-business-sharing-theme01-v3-green-editable/goal.json`。
- 局部覆盖：`output/opc-business-sharing-theme01-v3-green-editable/custom/`。
- 本机预览：`http://127.0.0.1:5527/`。

`output/opc-business-sharing-theme07-v1/` 和 `HTML演示稿/` 均保留为旧版对照，不覆盖、不删除。

## 页面结构

1. 封面：居中标题，无页码，显示“主讲人：孙轩”。
2. 个人介绍：电台和斗鱼照片各自上下拼接为竖图，武汉驾到使用原始竖图。
3. 案例背景：武汉驾到已有基础、原有方式与增长缺口，表格统一为 `24px` 字号。
4. 线上运营目标：改为大号绿色章节标题，围绕获客、咨询和成交展开。
5. 公域到私域获客链路：五层 HTML 漏斗，文字可编辑。
6. 转化效率优化：与第 5 页使用相同尺寸和几何结构，文字可编辑。
7. 整体回顾：运营目标、获客链路、转化优化、业务结果四步递进。
8. 现场交流与答疑：仅保留大标题和微信二维码，无黑色标签、竖线或其他装饰。

## 验证结果

- `props:safe`：8 页全部通过，0 错误、0 警告、无版式替换。
- `validate:goal-spec`：通过，8 个页面组件均唯一。
- `validate:swiss`：通过，识别 8 页。
- `validate:goal-copy`：通过，无未覆盖模板文案。
- 媒体检查：三张个人阶段图片和一张微信二维码均已归档到 `ppt/assets/user-media/` 并正常显示。
- 中文检查：页面可见文案未保留双语标签或模板英文。
- 视觉检查：通过 Dashi PDF 导出并逐页转图检查；8 页无明显溢出、遮挡或裁切，第 5、6 页漏斗尺寸一致，第 8 页装饰已清理。
- 浏览器边界：本轮内置浏览器无可用会话，因此未完成浏览器内手动编辑与自动保存测试；HTML 技术校验、脚本执行和 PDF 渲染检查均已完成。

## 重新渲染注意

第 3–6 页及第 8 页使用当前版本独立的局部覆盖。若重新运行 Dashi 渲染，需再次执行：

```bash
node output/opc-business-sharing-theme01-v3-green-editable/postprocess-green-editable.mjs
```

## 后续素材方式

用户可以直接给出本地图片或二维码路径，不需要先手动复制进演示稿目录。收到素材后，使用 Dashi `media:stage` 归档到当前版本，再重新渲染和校验。

涉及用户评价、咨询记录、微信聊天和成交信息的截图，必须先确认公开边界并脱敏；不自行补造图片、转化率或业务结果。

## 当前判断

| 已确认事实 | 不确定的部分 | 建议采取的动作 |
| --- | --- | --- |
| 8 页结构、绿色风格、个人照片、等字号表格、等尺寸漏斗和二维码均已落地 | 第 2 页阶段文字是否还需要微调 | 先看当前预览，再按实际讲述逐页修改 |
| 第 5、6 页为原生 HTML 文本，不是图片 | 浏览器编辑后的自动保存尚未实测 | 在系统浏览器打开预览后试改一处漏斗文字 |
| 结构、素材、局部覆盖和验证结果均已归档 | 是否需要最终导出 PDF 或 PPTX 备份尚未决定 | 内容稳定后再统一导出，避免重复版本 |
