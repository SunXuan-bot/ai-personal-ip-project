# 2026-07-19 进度同步说明

## 本日完成

- 基于 `05_发布与数据/0719/成品/成品.srt` 梳理 Codex 装皮肤视频结构。
- 形成内容时间线：`05_发布与数据/0719/内容时间线.md`。
- 完成顶部章节时间戳效果多轮迭代，最终版为本地文件：`D:\个人\自媒体\05_发布与数据\0719\成品\成品_带节点时间戳进度轨迹.mp4`。
- 将最终时间戳能力沉淀为项目技能：`.agents/skills/video-timestamp/`。

## Git 同步范围

本次同步进入 GitHub 的内容包括：

- 0719 字幕与时间线文档。
- 0719 时间轴 ASS / ffmpeg 生成文件。
- 复现用章节文件：`05_发布与数据/0719/chapters.json`。
- 项目技能“视频时间戳”：`.agents/skills/video-timestamp/`。

视频 MP4 本体未纳入 Git：

- 原片 `成品.mp4` 约 319MB，超过 GitHub 普通单文件限制。
- 多个迭代版 MP4 各约 60MB，属于生成媒体文件，当前 `.gitignore` 也默认忽略。
- 如公司电脑需要视频本体，建议用网盘、移动硬盘或后续配置 Git LFS。

## 公司电脑复现方式

如果公司电脑拿到了原视频，可运行：

```powershell
python .agents/skills/video-timestamp/scripts/render_video_timestamps.py `
  --input "D:\path\成品.mp4" `
  --chapters "05_发布与数据\0719\chapters.json" `
  --output "D:\path\成品_带节点时间戳进度轨迹.mp4"
```

## 对抗式审查

- 不要把大视频直接强塞进普通 Git 仓库，尤其是超过 100MB 的原片。
- 明天拉仓库后，要先确认 `.agents/skills/video-timestamp/` 是否存在；如果工作区默认隐藏 `.agents`，需要手动查看隐藏目录。
- 复现时要确认本机有 `ffmpeg`、`ffprobe` 和 Python Pillow。
- 若章节文案后续调整，应先改 `chapters.json`，再重新渲染。
